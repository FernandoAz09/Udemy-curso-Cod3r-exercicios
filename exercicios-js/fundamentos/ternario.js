const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' // Primeira parte Expressão/ ? Segunda parte retorno true / : terceira parte retorno false

console.log(resultado(7.1))
console.log(resultado(6.9))
